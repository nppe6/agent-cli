# Tasks

Store task-scoped PRDs, implementation context, review context, and task state here.

The default install includes `00-bootstrap-guidelines/`, a first-run task that asks the AI to inspect the codebase and fill `.shelf/spec/` with real project conventions. Complete or archive it once the specs contain useful examples.
